<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
          PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
          "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
      
<mapper namespace="dao.ShareNoteDao">
	<insert id="addShareNote" parameterType="entity.ShareNote">
		insert into share_note values(
		#{share_id},#{share_title},#{share_body},#{note_id})
	</insert>

</mapper>